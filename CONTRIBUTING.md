# Contributing to gaze-caret

## 作業を始める

[要件](docs/requirements.md)、[検証計画](docs/validation-plan.md)、[開発方針](docs/development-strategy.md) を確認し、今回変える振る舞いと対象の ID を PR に記す。

通常は最新の `develop` から短い作業ブランチを作り、PR の base も `develop` にする。`main` はリリースの基準。依存 PR が未統合なら、そのブランチを一時的な base にする Draft PR とし、親 PR と最終的な統合先を明記する。

## Red → Green → Refactor

1. **受け入れ例を選ぶ。** 小さい入力、期待する出力・失敗理由、要件 ID を決める。境界時刻、欠損、停止など、誤ると実測の信頼性を損なう例を優先する。
2. **Red。** その振る舞いのテストを先に書いて実行する。未実装の動作が理由で失敗したことを確認する。構文エラー、依存不足、テストの検出漏れは振る舞いの Red と区別する。
3. **Green。** その例を満たす最小の製品コードを実装し、対象のテストを成功させる。成功させるために期待値を弱めたり、テストを skip したりしない。
4. **Refactor。** 重複や責務を必要な範囲で整理し、テストを再実行する。新しい振る舞いは次の Red から追加する。整理が不要なら、その旨を記録して次の例へ進む。
5. **レビュー可能な状態にする。** 関連する既存テスト、型検査、該当するブラウザ・実機確認を行う。実施したものと未実施のものを分けて PR に記す。

失敗したテストのコマンド・失敗理由と、対応する成功結果を PR または短い実装記録に残す。Red の commit を必ず共有する必要はない。共有する場合は作業ブランチ上の履歴とし、統合時の head は必要な検証が成功していること。

機能追加と不具合修正はこの手順を標準とする。文書の修正にはリンクや内容の確認、設定変更にはその設定が使われる実行結果など、変更に合った検証を行う。TDD の体裁だけを作るテストは追加しない。

## テストの境界

- 純粋な計算は、固定入力と独立した期待値で調べる。フレーム時刻を引数で渡し、実時間の sleep や実カメラをユニットテストへ持ち込まない。
- カメラ・時計・モデル読み込みなどの外部境界には制御可能な代替を使い、停止や遅い応答の競合を再現する。
- DOM の行矩形、Selection、キー競合、権限、WASM 読み込みは、必要になった段階で実ブラウザのテストを追加する。Node の成功をブラウザ動作の証明にしない。
- 視線精度と使いやすさは [検証計画](docs/validation-plan.md) に従って本人の PC で測る。自動テストの件数や網羅率とは別の結果として報告する。

## 方式を調べる spike

`spike/<question>` の開始時に、問い、比較する方式、固定条件、打ち切る時間または試行数を決める。終了時は、採用・改善・保留と根拠を残す。使い捨てコードのすべてに TDD を要求しないが、製品へ持ち込む経路は受け入れ例を先に定める。

実験パラメータは明示的な設定とし、モデルや評価器に散らさない。同じ正式評価セッション中に設定や学習データを変更しない。

## PR の完了条件

- 対象要件、変更によって可能になること、範囲外が説明されている。
- 必要なテスト・型検査が成功し、機能追加・修正には Red と Green の実行証拠がある。
- 再現手順、設定変更、残る制約が分かる。
- カメラ・ブラウザ・視線精度に関する未確認事項を、確認済みとして報告していない。
- base が `develop`、または説明付きの依存 PR のブランチである。リリース以外で `main` へ向けない。

リポジトリ所有者は差分と検証結果を確認して統合する。1 人運用では自分以外の承認者を必須にするルールを前提としない。ブランチ保護の強制と、この作業手順の合意は別に管理する。
